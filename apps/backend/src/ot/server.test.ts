import { describe, expect, test, jest } from "@jest/globals";
import { Server } from "./server";
import { getRowsByPage } from "./data-grid";

describe("Client module", () => {

  test("test the `receiveOperation` method.", () => {
  });
});
