[package]
name = "core-wasm"
version = "0.1.0"
edition = "2021"

[dependencies]
wasm-bindgen = "0.2.93"

[lib]
crate-type = ["cdylib"]  # 指定生成 cdylib 类型的动态库

[package.metadata.wasm-pack.profile.release]
wasm-opt = false